import 'package:flutter/material.dart';

class ModdleBanner extends StatelessWidget {
  const ModdleBanner({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
